# Asio consumer test project: standalone, header-only, cross-platform behavior.
[package]
name = "asio-tests"
version = "0.1.0"

[indices]
compat = { path = "../../.." }

[dependencies.compat]
asio = "1.38.1"
